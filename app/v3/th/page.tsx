import SiteDesktop from '../SiteDesktop';

export const dynamic = 'force-dynamic';

// Preview of the rebuilt desktop (Thai).
export default function V3ThPage() {
  return <SiteDesktop lang="th" base="/v3" />;
}
